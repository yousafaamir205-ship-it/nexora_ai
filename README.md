Build me a clean multi page website called NEXORA for a web engineering assignment. It should have a homepage, about page, contact page, sign up page, and sign in page, all linked together with a simple navigation and a polished modern look. Use plain HTML with Tailwind from a CDN and Flowbite style components so it looks professional without needing a build step.

On the contact page, make a working form that sends to Formspree and shows a friendly success message after submit, but still works if JavaScript is off. The sign up and sign in pages can be front end only, just basic input validation and helpful messages, no real backend needed. Add nice sections, reusable styling, responsive layout, and place holder areas for images and a map that I can replace later. Keep everything static and ready to open locally or deploy to GitHub Pages. If you need to check anything, look up current docs online.

├── assets/ (2000 tokens)
    ├── images/
    │   └── .gitkeep
    ├── css/ (800 tokens)
    │   └── style.css (800 tokens)
    └── js/ (1200 tokens)
    │   └── script.js (1200 tokens)
├── README.md (200 tokens)
├── src/ (10600 tokens)
    └── pages/ (10600 tokens)
    │   ├── signin.html (2400 tokens)
    │   ├── signup.html (2400 tokens)
    │   ├── contact.html (2600 tokens)
    │   └── about.html (3200 tokens)
└── index.html (4400 tokens)
